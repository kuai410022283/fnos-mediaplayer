[
    {
        "stepTitle": "${install.stepTitle}",
        "items": [
            {
                "type": "tips",
                "helpText": "${install.helpText}"
            },
            {
                "type": "text",
                "field": "wizard_data",
                "label": "${install.dataDirLabel}",
                "initValue": "/vol1/1000/docker/mediaplayer",
                "rules": [
                    { "required": true, "message": "${install.dataDirRequiredMsg}" }
                ]
            },
            {
                "type": "text",
                "field": "server_port",
                "label": "${install.portLabel}",
                "initValue": "9527",
                "rules": [
                    { "required": true, "message": "${install.portRequiredMsg}" },
                    { "pattern": "^[0-9]+$", "message": "${install.portPatternMsg}" },
                    { "min": 1, "max": 65535, "message": "${install.portRangeMsg}" }
                ]
            },
            {
                "type": "password",
                "field": "admin_password",
                "label": "${install.passwordLabel}",
                "initValue": "admin123",
                "rules": [
                    { "required": true, "message": "${install.passwordRequiredMsg}" },
                    { "min": 6, "max": 64, "message": "${install.passwordLengthMsg}" }
                ]
            }
        ]
    }
]
